const sendEmail = async (options: any) => {
    // Skeleton for email sending
    console.log(`Email would be sent to ${options.email} with subject: ${options.subject}`);
};
export default sendEmail;
